WITH P AS (
    SELECT ID
    FROM ECOLI_DATA
),

C AS (
    SELECT PARENT_ID
    FROM ECOLI_DATA
),

P_C AS (
    SELECT ID
         , COUNT(PARENT_ID) CHILD_COUNT
    FROM P
    LEFT JOIN C
    ON ID = PARENT_ID
    GROUP BY ID
)

SELECT *
FROM P_C
ORDER BY ID
;